# وكيل BugIt لضمان الجودة — نظرة عامة

<bdi dir="ltr">BugIt</bdi> هو وكيل تجاري لـ<bdi dir="ltr">VS Code Copilot</bdi> يحوّل ملاحظات الاختبار الأولية إلى تقارير خلل متسقة. يصوغ المسودات محليًا في مساحة عملك ولا يكتب إلى الخدمات المتصلة إلا بعد المعاينة والتأكيد.

## سير العمل الأساسي

- التقاط ملاحظات إعادة الإنتاج الأولية والسجلات ولقطات الشاشة والسلوك المتوقع.
- صياغة تقرير منظّم يتضمن العنوان ودرجة الخطورة والبيئة والخطوات والأدلة.
- البحث في نظام التتبع المتصل عن تذاكر مكررة محتملة.
- معاينة الوجهة والمحتوى النهائي والموافقة عليهما قبل أي كتابة خارجية.
- إضافة تعليقات التحقق بعد إعادة اختبار الإصلاح.

## الخصوصية والتحكم

- لا يرسل <bdi dir="ltr">BugIt</bdi> أي تحليلات منتج أو بيانات قياس تذاكر إلى <bdi dir="ltr">Taskivator</bdi>.
- يعالج مزوّد الذكاء الاصطناعي المتصل والتكاملات المفعّلة المحتوى الذي تختار إرساله فقط.
- تستخدم طلبات الترخيص والتحديث بيانات الترخيص ومعرّف جهاز أحادي الاتجاه، وليس محتوى التذاكر.
- يمنع وضع المحاكاة (<bdi dir="ltr">dry-run</bdi>) أدوات <bdi dir="ltr">Python</bdi> المضمّنة من الكتابة، لكن يجب أن تراجع إجراءات <bdi dir="ltr">MCP</bdi> الخارجية بنفسك.
- يجب ألا تحتوي ملفات الإعداد على قيم بيانات الاعتماد مطلقًا.

## مستويات التكامل

- تعيين حقول مضمّن ومُختبَر: <bdi dir="ltr">Jira Cloud</bdi> و<bdi dir="ltr">Azure DevOps</bdi>، يُودَعان مباشرة عبر واجهة <bdi dir="ltr">REST API</bdi> لنظام التتبع باستخدام رمز وصول تنشئه في حسابك.
- مصدر معرفة موجّه: <bdi dir="ltr">Confluence Cloud</bdi> عبر <bdi dir="ltr">Atlassian Rovo MCP</bdi>، مع تسجيل الدخول في المتصفح.
- تجريبي مع فحص مباشر: <bdi dir="ltr">Sentry</bdi> و<bdi dir="ltr">GitHub</bdi> و<bdi dir="ltr">Linear</bdi> و<bdi dir="ltr">Notion</bdi>.
- إرشادات إعداد فقط: خوادم متوافقة توفرها مؤسستك لأنظمة التتبع الأخرى وأدوات التعطل وإدارة الاختبارات والتواصل وخدمات المعرفة.
- غير مدعومة بالإعداد التلقائي: موصلات <bdi dir="ltr">S3</bdi> و<bdi dir="ltr">Google Drive</bdi> و<bdi dir="ltr">Azure Blob</bdi>.

## نطاق الإصدار

- <bdi dir="ltr">BugIt</bdi> هو الإصدار التجاري المنشور الحالي، ويخضع للصيانة النشطة.
- يُعد <bdi dir="ltr">Windows 11</bdi> و<bdi dir="ltr">VS Code</bdi> و<bdi dir="ltr">GitHub Copilot</bdi> و<bdi dir="ltr">Python 3.10</bdi> إلى <bdi dir="ltr">3.13</bdi> البيئة المعتمدة للإصدار.
- يتوفر دليل المستخدم الكامل والنظرة العامة بصيغة <bdi dir="ltr">PDF</bdi> بالإنجليزية وبكل لغة مدعومة — يمكنك معاينتهما أو تنزيلهما أدناه.

## السياسات

- اقرأ [بيان الخصوصية](/public/docs/PRIVACY.ar.md).
- راجع [إرشادات الأمان](/public/docs/SECURITY.md).

================================================================================
DOCUMENT 3 OF 6
================================================================================
Source: .cache/_website_lqa_sources/raw/en/LICENSE.txt
